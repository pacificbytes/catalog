# Public keys (safe to expose to browser)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Used for redirects in some routes (e.g., signout). Set to your deployed URL.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Private
# Email of the single admin account that can access /admin
ADMIN_EMAIL=
